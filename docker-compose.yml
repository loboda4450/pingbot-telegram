version: '3.7'

services:
  bot:
    build: .
    volumes:
      - ./config.yml:/app/config.yml:ro
      - ./utils/users-orm.db:/app/utils/users-orm.db
      - ./PingBot.session:/app/PingBot.session
    restart: unless-stopped